"""Cycle-cost data for the performance model (ROADMAP.md section I).

Nothing in the execution path consumes this package. It is the data half of
the cycle-approximate estimator, landed ahead of the machinery that will use
it: Phase 5 of ``docs/plans/event-driven-pump.md``. See
``docs/plans/cost-model.md``.
"""
